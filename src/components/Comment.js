import React from "react";
import PropTypes from "prop-types";
import CommentDetail from "./CommentDetail";

const Comment = props => {
  return (
    <div>
      <CommentDetail props={this.props} />;
      <CommentDetail props={this.props} />;
      <CommentDetail props={this.props} />;
    </div>
  );
};

export default Comment;
